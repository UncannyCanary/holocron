{
  "name": "holocron",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.18.0",
  "scripts": {
    "dev": "pnpm --filter @holocron/shared build && dotenv -e .env -- pnpm --parallel -r --filter=./apps/* run dev",
    "test": "pnpm --filter @holocron/shared build && vitest run",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.12",
    "dotenv-cli": "11.0.0",
    "pdf-lib": "^1.17.1",
    "vitest": "5.0.0"
  }
}
