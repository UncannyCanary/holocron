{
  "name": "holocron",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.18.0",
  "scripts": {
    "dev": "dotenv -e .env -- pnpm --parallel -r --filter=./apps/* run dev",
    "test": "vitest run",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.12",
    "dotenv-cli": "11.0.0",
    "vitest": "5.0.0"
  }
}
