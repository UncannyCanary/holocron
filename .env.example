# Copy this file to .env and fill in real values. .env is never committed.

# Postgres, used by the API, the worker, and pg-boss.
DATABASE_URL=postgres://holocron:holocron@localhost:5432/holocron

# Port the API listens on locally. Compose sets this itself for its own services.
PORT=3000

# Where the OCR language file is kept. Inside Docker this is the /data volume.
OCR_CACHE_DIR=.scratch/data/tesseract

# Where uploaded files and rendered page images are kept. Inside Docker this
# is the /data volume.
DATA_DIR=.scratch/data

# Signs the hc_ws workspace cookie. Any long random string works for local
# runs; use a real secret in production.
COOKIE_SECRET=dev-secret-change-me

# The key for the model calls. Get one from the Claude console. Without it
# the API and the worker run, but no document can be read.
ANTHROPIC_API_KEY=

# The model that reads documents. Leave it unset for claude-opus-5. Set it to
# claude-sonnet-5 to run about two and a half times cheaper.
# EXTRACTION_MODEL=

# The model that says how many documents a file holds before any is read.
# Leave it unset for claude-haiku-4-5-20251001, which is enough for that.
# SPLIT_MODEL=

# The one shared code the door asks for. Leave this unset to open the site
# with no door at all.
# ACCESS_CODE=
